REACT_APP_API_BASE_URL=http://localhost:4000/api
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=

# DEV-ONLY: when "true", every signed-in user is treated as admin in the UI.
# Pair with backend ADMIN_ROLE_BYPASS=true. Disable in production.
REACT_APP_ADMIN_BYPASS=false
